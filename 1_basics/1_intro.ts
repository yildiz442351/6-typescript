/* 
* Typescript, js'nin bazı önemli açıklarını düzeltmek için 
*microsoft tarafından geliştirilen bir proğramlama dilidir.

* Açıklar:
* * Js'de tanımladığımız bir değişken daha sonra tanımlanan dan farklı tipte bir veri atayabiliyor.
* * otomatik tamamlama desteği yok
* * Js'de kodu çalıştırmadan hataları göremiyoruz
* * İstedğimiz sürüme derleme özelliği yok 

* Typescript dosyaları doğrudan çalıştırılmazlar. Eğer dosya çalıştırmak istiyorsak 
* yazılan ts kodunu derlememiz gerekiyor

*TSC: Typscript Compllier 
* yazılan ts kodunu js'in istediğimiz sürüme derler
* kullanabilmek için typescript paketini indirmemiz gerekir
*/

const first_name = 'yildiz';

console.log(first_name);